@inject NavigationManager _navigation 

@if(Inventory != null)
{
<tr>
          <td>@Inventory.InventoryName</td>
          <td>@Inventory.Quantity</td>
          <td>@Inventory.Price.ToString("c")</td>
          <td>
          <button type="button" class="btn btn-secondary" @onclick="@( () => EditInventory(@Inventory.InventoryId) )">Edit</button>
          </td>
  </tr>  
}

@code {

    [Parameter]
    public Inventory Inventory { get; set; }

    private void EditInventory(int id)
    {
        _navigation.NavigateTo($"/editinventory/{id}");
    }
}
